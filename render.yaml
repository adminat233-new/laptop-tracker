services:
  - type: web
    name: laptop-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: TRACKER_PIN
        value: "1234"
      - key: NODE_ENV
        value: production
